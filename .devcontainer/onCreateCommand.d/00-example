#!/bin/zsh -eu

# 这是一个示例脚本，用于演示如何在 Devcontainer 创建时自动执行命令。
#
# 您可以创建任意数量的脚本，它们将按照文件名的字母顺序依次执行。
#
# 推荐的命名规范是：`{两位数字}-{描述性名称}`，例如 `10-install-deps`。
#
# - 脚本文件名不能包含任何点号 (.)，否则 run-parts 将会忽略它。
# - 脚本会自动被赋予可执行权限，您无需手动设置。
#
# 在这里添加您自己的命令：
echo "--- 正在执行您的自定义示例脚本 ---"

# 修复 node_modules 权限问题:
sudo chown -R $(whoami):$(whoami) node_modules || true

# 调用内置命令 (这些命令在 onCreateCommand-internal.d 目录中的脚本中定义)
h-setup-locale
h-setup-zsh
h-setup-bun

# 或者安装一个新的系统软件包:
# apt-get update && apt-get install -y cowsay
