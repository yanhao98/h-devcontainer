# Dev Container Environment

This dev container includes a lightweight Fluxbox-based desktop that can be accessed using a VNC viewer or the web.

## GUI Access

- GUI-based commands executed from the terminal will open on the desktop automatically
- Access the desktop via VNC viewer or web browser

## Tips

- Run GUI applications directly from the terminal
- The Fluxbox window manager provides a minimal desktop environment
