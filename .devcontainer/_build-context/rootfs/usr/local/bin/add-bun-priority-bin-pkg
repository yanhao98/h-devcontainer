#!/bin/bash
set -o errexit    # 对应 -e
set -o nounset    # 对应 -u
set -o pipefail   # 对应 -o pipefail

echo "📦 安装: $*" >&2
mkdir -p /vscode/bun-priority-bin/

BUN_ARGS=()
BUN_ARGS+=("--silent")
BUN_ARGS+=("--no-env-file")
BUN_ARGS+=("--no-progress")
BUN_ARGS+=("--no-summary")
BUN_ARGS+=("--cwd=/vscode/bun-priority-bin/")

exec bun "${BUN_ARGS[@]}" add "$@"
