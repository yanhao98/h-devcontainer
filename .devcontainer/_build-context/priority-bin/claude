#!/bin/bash
set -o errexit    # 对应 -e
set -o nounset    # 对应 -u
set -o pipefail   # 对应 -o pipefail
# https://github.com/anthropics/claude-code/blob/1fe9e369a7c30805189cbbb72eb69c15ed4ec96b/.devcontainer/Dockerfile#L42
# https://code.claude.com/docs/zh-CN/settings#可用设置
# https://code.claude.com/docs/zh-CN/settings#环境变量
# https://code.claude.com/docs/zh-CN/statusline

#-------------------------------------------------------------------------------------------------------------
# ANTHROPIC_AUTH_TOKEN
# ANTHROPIC_BASE_URL

# 直接指定当前使用的模型
# ANTHROPIC_MODEL

# 用于 haiku 的模型，或后台功能
# ANTHROPIC_DEFAULT_HAIKU_MODEL=claude-haiku-4-5-20251001

# 用于 opus 的模型，或在计划模式活跃时用于 opusplan 的模型。
# ANTHROPIC_DEFAULT_OPUS_MODEL=claude-opus-4-5-20251101

# 用于 sonnet 的模型，或在计划模式不活跃时用于 opusplan 的模型。
# ANTHROPIC_DEFAULT_SONNET_MODEL=claude-sonnet-4-5-20250929

# CLAUDE_CODE_SUBAGENT_MODEL	用于子代理的模型

# "apiKeyHelper": "/bin/generate_temp_api_key.sh",
#-------------------------------------------------------------------------------------------------------------


# 获取并打印调用者信息
PARENT_INFO=$(ps -p $PPID -o args= 2>/dev/null || echo "unknown")
echo "🔍 调用者: $PARENT_INFO (PID: $PPID)"

# 解析命令行参数，检查是否有 --skip-settings
CLAUDE_ARGS=()
for arg in "$@"; do
  if [[ "$arg" == "--skip-settings" ]]; then
    export CLAUDE_SKIP_SETTINGS=1
    echo "⏭️  检测到 --skip-settings 参数"
  else
    CLAUDE_ARGS+=("$arg")
  fi
done
set -- "${CLAUDE_ARGS[@]}"

echo "🚀 正在启动 Claude Code 包装器 ($0)..."

echo "📦 正在检查并安装 @anthropic-ai/claude-code@latest..."
mkdir -p /vscode/bun-priority-bin/
bun --no-progress --cwd=/vscode/bun-priority-bin/ add @anthropic-ai/claude-code@latest


# 初始化 Claude Code 配置文件
init_claude_config() {
  local config_file="/vscode/claude-config/.claude.json"
  local config_content='{
  "numStartups": 0,
  "theme": "light-daltonized",
  "bypassPermissionsModeAccepted": true,
  "hasCompletedOnboarding": true
}'

  if [[ -f "$config_file" ]]; then
    # echo "Claude Code 配置文件 (.claude.json) 已存在，跳过覆盖。"
    :
  else
    mkdir -p "$(dirname "$config_file")"
    echo "$config_content" > "$config_file"
    echo "⚙️  已初始化配置文件: $config_file"
  fi
}

# 选择 settings 文件
# 设置环境变量 CLAUDE_SKIP_SETTINGS=1 可跳过选择
# 或使用命令行参数 --skip-settings
select_settings_file() {
  if [[ "${CLAUDE_SKIP_SETTINGS:-}" == "1" ]]; then
    echo "⏭️  已跳过选择 settings 文件 (CLAUDE_SKIP_SETTINGS=1)"
    return
  fi

  # 调用独立的 claude-switch-settings 脚本
  # 使用 source 以便获取 SELECTED_SETTINGS_FILE 环境变量
  local switch_script
  switch_script="$(get_switch_settings_path)"
  if [[ -n "$switch_script" ]]; then
    source "$switch_script"
  else
    echo "⚠️  警告: 未找到 claude-switch-settings 脚本"
  fi
}

# 获取 claude-switch-settings 脚本的路径
get_switch_settings_path() {
  local script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
  if [[ -x "$script_dir/claude-switch-settings" ]]; then
    echo "$script_dir/claude-switch-settings"
  elif command -v claude-switch-settings &>/dev/null; then
    command -v claude-switch-settings
  fi
}

# 获取 claude-statusline 脚本的路径
get_statusline_path() {
  local script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
  if [[ -x "$script_dir/claude-statusline" ]]; then
    echo "$script_dir/claude-statusline"
  elif command -v claude-statusline &>/dev/null; then
    command -v claude-statusline
  else
    echo "claude-statusline"
  fi
}

# 设置 Claude Code 的托管配置
setup_managed_settings() {
  local announcements=()
  local target_settings_file="/vscode/claude-config/settings.json"

  if [[ -f "$target_settings_file" ]]; then
    local extracted_url
    local extracted_model
    
    extracted_url=$(jq -r '.env.ANTHROPIC_BASE_URL // empty' "$target_settings_file" 2>/dev/null)
    extracted_model=$(jq -r '.env.ANTHROPIC_MODEL // empty' "$target_settings_file" 2>/dev/null)
    
    if [[ -n "$extracted_url" ]]; then
      announcements+=("🚀 ANTHROPIC_BASE_URL: $extracted_url")
    fi
    if [[ -n "$extracted_model" ]]; then
      announcements+=("🤖 ANTHROPIC_MODEL:    $extracted_model")
    fi
  fi

  if [[ -n "${SELECTED_SETTINGS_FILE:-}" ]]; then
      announcements+=("📄 $(basename "$SELECTED_SETTINGS_FILE")")
  fi

  # 如果没有信息，提示用户
  if [[ ${#announcements[@]} -eq 0 ]]; then
    announcements+=("⚠️ 未找到配置信息，请检查 settings 文件。")
  fi

  # 合并成一个字符串，用 \n 分隔
  # 查找 claude-switch-settings 脚本的实际路径
  local switch_script_path
  switch_script_path="$(get_switch_settings_path)"
  
  local combined_announcement=""
  local separator="─────────────────────────────────────────────"
  if [[ -n "$switch_script_path" ]]; then
    combined_announcement="💡 运行 $switch_script_path 切换配置\n$separator"
  else
    combined_announcement="💡 运行 claude-switch-settings 切换配置\n$separator"
  fi
  for item in "${announcements[@]}"; do
    combined_announcement="${combined_announcement}\n${item}"
  done
  
  # 转义双引号
  combined_announcement="${combined_announcement//\"/\\\"}"

  # 获取 claude-statusline 的实际路径
  local statusline_path
  statusline_path="$(get_statusline_path)"

  local settings_json='{
  "companyAnnouncements": ["'"$combined_announcement"'"],
  "defaultMode": "bypassPermissions",
  "statusLine": {
    "type": "command",
    "command": "'"$statusline_path"'",
    "padding": 0
  }
}'
  
  sudo mkdir -p /etc/claude-code
  echo "$settings_json" | sudo tee /etc/claude-code/managed-settings.json > /dev/null
  sudo chmod 666 /etc/claude-code/managed-settings.json
  echo "⚙️  已生成托管配置: /etc/claude-code/managed-settings.json"
}

init_claude_config
# select_settings_file
setup_managed_settings


BUN_ARGS=("--bun")
BUN_ARGS+=("run")
BUN_ARGS+=("/vscode/bun-priority-bin/node_modules/.bin/claude")

CLI_ARGS=("--dangerously-skip-permissions")

echo "🚀 正在执行: bun ${BUN_ARGS[*]} ${CLI_ARGS[*]} $*"
# bun --bun --cwd=/vscode/bun-priority-bin/ run claude --version

# 从 PATH 中移除当前脚本所在的目录，防止无限递归
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# 同时移除 /usr/local/bin-priority，因为这是安装后的位置
export PATH=$(echo "$PATH" | sed "s|$SCRIPT_DIR||g; s|/usr/local/bin-priority||g; s|::|:|g; s|^:||; s|:$||")

exec bun "${BUN_ARGS[@]}" "${CLI_ARGS[@]}" "$@"
